import NSDataModel from "./base-model.mjs";

export default class NSItemBase extends NSDataModel {

  static defineSchema() {
    const fields = foundry.data.fields;
    const schema = {};

    schema.description = new fields.StringField({ required: true, blank: true });

    return schema;
  }

}